# Dependances de l'IMAGE CONTENEUR (Mission 004.3.8): l'extra `persistence` seul, sans outil de test.
# Sous-ensemble STRICT de requirements.lock: memes lignes, octet pour octet (tests/test_container_artifacts.py).
# Installe par le Dockerfile:
#     pip install --no-deps --only-binary=:all: -r requirements-runtime.lock
# Fermeture transitive de psycopg[binary] et alembic sur Linux (marqueurs evalues sur la plateforme).
alembic==1.20.0
greenlet==3.5.6 ; platform_machine == "aarch64" or platform_machine == "ppc64le" or platform_machine == "x86_64" or platform_machine == "amd64" or platform_machine == "AMD64" or platform_machine == "win32" or platform_machine == "WIN32"
Mako==1.4.1
MarkupSafe==3.0.3
psycopg==3.3.5
psycopg-binary==3.3.5 ; implementation_name != "pypy"
SQLAlchemy==2.0.53
typing_extensions==4.16.0
