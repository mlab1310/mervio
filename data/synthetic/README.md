# data/synthetic/

Jeux **synthetiques** generes par `scripts/generate_data.py`. Non versionnes (voir `data/README.md`).
Chaque jeu porte un `manifest.json` avec `synthetic: true` et `not_for_production: true`.
